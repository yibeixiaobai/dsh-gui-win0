{
  "$schema": "../gen/schemas/desktop-capabilities.json",
  "identifier": "default",
  "description": "Minimal capability set: native dialog only; renderer has no filesystem or shell access.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
